@model Oblig1.Models.Eier

<h3>Are you sure you want to delet this order!</h3>
<div>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">Person ID</dt>
        <dd class="col-sm-10">@Model.Person.Id</dd>

        <dt class="col-sm-2">Account Number</dt>
        <dd class="col-sm-10">@Model.kontoNummer</dd>

        <dt class="col-sm-2">Amount of listings </dt>
        <dd class="col-sm-10">@Model.antallAnnonser</dd>

       

    </dl>

    <form asp-action="SlettBekreftet" asp-route-id="@Model.kontoNummer" method="post">
        <button type="submit" class="btn btn-danger">Slett</button>
        <a asp-controller="Eier" asp-action="Tabell" class="btn btn-secondary">Avbryt</a>
    </form>
</div>